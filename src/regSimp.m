% simply train data for some test runs before the real thing

% files had to be seperated because matlab signifi digits cut them off
train = train(1:100, :);
trainID = trainID(1:100, :);
trainY = trainY(1:100, :);
